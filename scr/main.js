const searchHTML = document.querySelector(".search")
const btnHTML = document.querySelector(".btn")
const inputHTML = document.querySelector(".input")

btnHTML.addEventListener("click", () => {
    searchHTML.classList.toggle("active")
inputHTML.focus})